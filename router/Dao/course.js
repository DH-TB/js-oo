const connection = require('/connect');
const Course = require('../public/src/course');
const $sql = require('./sqlMapping');

module.exports = {
    choose: (req,res,next)=> {
    connection.query()
    }
};